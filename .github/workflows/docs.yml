name: 📚 Documentation

on:
  push:
    branches: [ main ]

jobs:
  docs:
    runs-on: ubuntu-latest
    timeout-minutes: 1
    
    steps:
    - name: 🚀 Checkout
      uses: actions/checkout@v4
      
    - name: 📝 Generate Docs
      run: |
        mkdir -p docs
        echo "# IXIMI Legacy Docs" > docs/index.md
        echo "Generated: $(date)" >> docs/index.md
        echo "✅ Docs created"
