name: 📚 Documentation

on:
  workflow_dispatch:  # SOLO manual, NO automático
  push:
    branches: [ main ]
    paths:
      - 'docs/**'
      - 'README.md'

jobs:
  generate-docs:
    name: 📝 Generate Documentation
    runs-on: ubuntu-latest
    timeout-minutes: 1  # ❗MÁXIMO 1 minuto, luego falla
    
    steps:
    - name: 🚀 Checkout
      uses: actions/checkout@v6
      with:
        fetch-depth: 1  # Solo último commit, más rápido
    
    - name: 📄 Create Minimal Docs
      run: |
        echo "📚 Generando documentación mínima..."
        
        # Solo crear si no existe
        mkdir -p docs
        
        # Archivo mínimo de 3 líneas
        echo "# IXIMI Legacy Docs" > docs/index.md
        echo "Last updated: $(date)" >> docs/index.md
        echo "Status: ✅ Active" >> docs/index.md
        
        echo "✅ Documentación creada en 0.5 segundos"
    
    - name: ✅ Quick Success
      run: |
        echo "🎉 Documentation workflow completed!"
        echo "⏱️  Time: < 5 seconds"
        echo "📁 Output: docs/index.md"
